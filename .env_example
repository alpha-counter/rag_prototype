DOMAIN = exampledomain.com
AUTH_SERVICE_URL = http://auth_service:8000
DATABASE_URL = postgresql+psycopg2://admin:admin@postgres/db
VECTORDB_URL = postgresql+psycopg2://admin:admin@postgres/vector
RETRIEVAL_SERVICE_URL = http://retrieval_service:8003
BRAND_NAME = examplebrand
OPENAI_API_KEY =
TAVILY_API_KEY =
SECRET_KEY =
# Generate a long random secret, e.g. `openssl rand -hex 32`
ACCESS_TOKEN_EXPIRE_MINUTES = 60

# Outbound mail configuration (set if password reset emails are enabled)
MAIL_USERNAME =
MAIL_PASSWORD =
MAIL_FROM =
MAIL_SERVER = smtp.example.com
MAIL_PORT = 587
MAIL_STARTTLS = true
MAIL_SSL_TLS = false
MAIL_USE_CREDENTIALS = true
MAIL_VALIDATE_CERTS = true
MAIL_FROM_NAME =

# AWS credentials for document/index services (S3, etc.)
AWS_ACCESS_KEY_ID =
AWS_SECRET_ACCESS_KEY =
AWS_DEFAULT_REGION = eu-west-2

CHAT_SERVICE_EXPORT_GRAPHS = false